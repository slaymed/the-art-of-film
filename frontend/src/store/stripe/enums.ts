export enum SessionStatus {
    PAID = "paid",
    UNPAID = "unpaid",
    REFUNDED = "refunded",
}

export enum SessionType {
    SUBSCRIPTION = "subscription",
    POSTER = "poster",
    ADVERTISMENT = "advertisement",
}
